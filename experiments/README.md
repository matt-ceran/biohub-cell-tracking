# Experiments

Use this directory for reproducible experiment records and small derived summaries.
Keep large generated artifacts in `data/working/` or `submissions/`.

Each experiment note should include the command, data split, runtime, score, artifact path, and conclusion.
